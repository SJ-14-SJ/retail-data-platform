name: retail_analytics
version: '1.0.0'
config-version: 2
profile: retail_analytics
model-paths: [models]
test-paths: [tests]
clean-targets: [target, dbt_packages]
models:
  retail_analytics:
    +materialized: view
